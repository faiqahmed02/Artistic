import React from "react";
import { View, Text } from "react-native";
import { withTheme } from "react-native-paper";

function ClassesArchive({ theme }) {
  return (
    <View>
      <Text>ClassesArchive</Text>
    </View>
  );
}

export default withTheme(ClassesArchive);
